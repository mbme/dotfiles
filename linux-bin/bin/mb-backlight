#!/usr/bin/env bash

# increase or decrease laptop backlight

if [ "$1" = "inc" ]; then
     xbacklight -inc 10 -time 50 -steps 1
elif [ "$1" = "dec" ]; then
     xbacklight -dec 10 -time 50 -steps 1
else
    echo "Acceptable params: inc, dec"
fi
