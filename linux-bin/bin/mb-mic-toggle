#!/usr/bin/env bash

if amixer | grep "Capture.*\[off\]"
then
    amixer sset Capture cap
    notify-send "Microphone enabled"
else
    amixer sset Capture nocap
    notify-send "Microphone disabled"
fi

exit 0
